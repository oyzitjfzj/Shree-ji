name: Supersede stale verification workload

on:
  push:
    branches: [main]
    paths:
      - "RUN_SUPERSEDE_STALE_571430C4"

permissions:
  contents: read

concurrency:
  group: opaque-tlc-diag-v4-00c0abcd-certpair
  cancel-in-progress: true

jobs:
  supersede:
    runs-on: ubuntu-24.04
    steps:
      - name: Release stale verification capacity
        run: echo "stale diagnostic concurrency group superseded"
